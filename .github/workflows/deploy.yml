name: Deployment Pipeline

on:
  push:
    branches:
      - develop

jobs:
  test-build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3


      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'

      - name: Install Dependencies
        run: go mod tidy

      - name: Run Tests
        run: go test ./... -v

      - name: Build Project
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o cs_streaming main.go
          ls -lah
      
      - name: Copy Binary to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_SSH_PASSWORD }}
          source: "app"
          target: "/home/frappe/cs_streaming/"

      - name: Restart Systemd Service on VPS
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_SSH_PASSWORD }}
          script: |
            echo '${{ secrets.VPS_SSH_PASSWORD }}' | sudo -S systemctl restart cs_streaming.service
            echo '${{ secrets.VPS_SSH_PASSWORD }}' | sudo -S systemctl status cs_streaming.service --no-pager